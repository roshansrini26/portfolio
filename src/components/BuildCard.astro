---
import type { Build } from "../data/builds";

interface Props {
  build: Build;
}
const { build } = Astro.props;
---

<div class:list={["card", { wide: build.wide }]}>
  <h3>{build.title}</h3>
  <p>{build.body}</p>
  <div class="stack">{build.stack.join(", ")}</div>
</div>
